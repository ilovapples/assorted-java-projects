## build
First, you'll want to compile `config_reader.c`, as `cc config_reader.c -o config_reader`.

Then, you can just go into a project directory and do whatever its `README.md` says to do for building.
